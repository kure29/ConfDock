name: Rust CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    name: Format, lint, and test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Install Rust 1.88.0 with quality components
        uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1
        with:
          toolchain: 1.88.0
          components: rustfmt, clippy

      - name: Check formatting
        run: cargo fmt --all --check

      - name: Run Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Run workspace tests with temporary SQLite databases
        run: cargo test --workspace

  web-quality:
    name: Web typecheck, test, and build
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: web
    steps:
      - name: Check out repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Node.js 22.14.0
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22.14.0
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install Rust 1.88.0 with the WASM target
        uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1
        with:
          toolchain: 1.88.0
          target: wasm32-unknown-unknown

      - name: Install wasm-bindgen CLI
        run: cargo install wasm-bindgen-cli --version 0.2.127 --locked

      - name: Install web dependencies
        run: npm ci

      - name: Build Rust WASM bindings
        run: npm run wasm:build

      - name: Typecheck web
        run: npm run typecheck

      - name: Test web
        run: npm run test

      - name: Build web
        run: npm run build

      - name: Audit web dependencies
        run: npm audit

      - name: Audit production dependencies
        run: npm audit --omit=dev

      - name: Check whitespace
        run: git diff --check

  single-binary-smoke:
    name: Single-binary smoke test
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Node.js 22.14.0
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22.14.0
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install Rust 1.88.0 with the WASM target
        uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1
        with:
          toolchain: 1.88.0
          target: wasm32-unknown-unknown

      - name: Install the pinned wasm-bindgen CLI
        run: cargo install wasm-bindgen-cli --version 0.2.127 --locked

      - name: Build the embedded single binary
        run: ./scripts/build-single-binary.sh

      - name: Run from a source-free temporary directory
        run: ./scripts/smoke-single-binary.sh target/confdock-rust-1.88.0/native/release/confdock

      - name: Package and re-test the downloadable archive
        run: |
          package_dir="$(mktemp -d)"
          trap 'rm -rf "$package_dir"' EXIT
          ./scripts/package-single-binary.sh target/confdock-rust-1.88.0/native/release/confdock "$package_dir"

  # Artifact publication is deliberately opt-in. Pull requests and pushes
  # execute the same packaging and extraction checks above without retaining
  # a downloadable artifact.
  single-binary-artifact:
    name: Build downloadable Linux x86_64 artifact
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 35
    permissions:
      contents: read
    steps:
      - name: Check out a clean repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Node.js 22.14.0
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22.14.0
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install the fixed Rust toolchain and WASM target
        uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1
        with:
          toolchain: 1.88.0
          target: wasm32-unknown-unknown

      - name: Install wasm-bindgen CLI matching Cargo.lock
        run: cargo install wasm-bindgen-cli --version 0.2.127 --locked

      - name: Install web dependencies
        run: npm ci
        working-directory: web

      - name: Build Rust WASM and Vite production assets
        run: |
          npm run wasm:build
          npm run build
        working-directory: web

      - name: Build the embedded Linux x86_64 binary
        run: ./scripts/build-single-binary.sh

      - name: Inspect Linux compatibility and build sizes
        run: |
          file target/confdock-rust-1.88.0/native/release/confdock
          ldd target/confdock-rust-1.88.0/native/release/confdock
          printf 'binary_bytes=%s\n' "$(wc -c < target/confdock-rust-1.88.0/native/release/confdock | tr -d ' ')"
          printf 'web_dist_bytes=%s\n' "$(find web/dist -type f -print0 | xargs -0 wc -c | tail -1 | awk '{print $1}')"
          printf 'wasm_bytes=%s\n' "$(find web/dist -type f -name '*.wasm' -print0 | xargs -0 wc -c | tail -1 | awk '{print $1}')"

      - name: Package, extract, verify SHA-256, and smoke-test
        run: ./scripts/package-single-binary.sh target/confdock-rust-1.88.0/native/release/confdock artifact

      - name: Upload Linux x86_64 artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: confdock-linux-x86_64
          path: |
            artifact/confdock-v*-linux-x86_64.tar.gz
            artifact/confdock-v*-linux-x86_64.tar.gz.sha256
          if-no-files-found: error
          retention-days: 7
